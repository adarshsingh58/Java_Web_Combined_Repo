package javaconcepts.multithreading.IntQuestions;

/**
 * Problem Statement
 * <p>
 * This is an actual interview question asked at Netflix.
 * <p>
 * Imagine we have an Executor class that performs some useful task asynchronously via the method
 * asynchronousExecution(). In addition the method accepts a callback object which implements the Callback interface.
 * the object’s done() gets invoked when the asynchronous execution is done. The definition for the involved classes is
 * below:
 *
 */
public class AsynchronoustoSynchronousProblem {
}
